#include <bits/stdc++.h>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t--)
	{
		int a,b,c;
		cin>>a>>b>>c;
		int ans;
		if((a>b && a<c) ||( a>c && a<b))
		ans=a;
		else if((a>c && b<c) || (b>c && a<c))
		ans=c;
		else
		ans=b;
		cout<<ans<<"\n";
	}
}
