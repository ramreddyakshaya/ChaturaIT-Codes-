#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin>>n;
    string str=to_string(n);
    //reverse(str.begin(),str.end());
    for(int i=0;i<str.size();i++)
    {
        cout<<str[i]-'0'<<"\n";
    }
}
