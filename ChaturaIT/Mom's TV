#include <bits/stdc++.h>
using namespace std;

int main() {
	int T;
	cin>>T;
	while(T--)
	{
		int a,b,c,d;
		cin>>a>>b>>c>>d;
		if(a-c==b-d)
		cout<<"Any"<<"\n";
		else if(a-c>b-d)
		cout<<"First"<<"\n";
		else
		cout<<"Second"<<"\n";
	}
	return 0;
}
